"""Competing aggregation methods (plan §5.2).

Two tiers — do NOT stack the deck with strawmen:
  weak/illustrative : naive mean, median, volume-weighted mean, reliability-weighted
                      (additive) mean, single-source-trust, trimmed mean.
  strong/real       : Dempster–Shafer combination, correlation-cluster-then-average,
                      robust aggregation (Huber / geometric-median). These are the bar
                      the method must actually clear (plan §6 gate #1, gate #5).
"""
